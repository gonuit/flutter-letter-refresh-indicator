name: example
description: Flutter letter indicator example

version: 0.1.0

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  letter_refresh_indicator:
    path: ../

flutter:
  uses-material-design: true
